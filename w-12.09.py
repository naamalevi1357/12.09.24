# start

mov: int = int(input("how long is the movie?"))
time = mov // 60
print(f"clok, {time}")
if not mov % 60 == 0:
    print(f"minutes {mov % 60}")

# stopp


# exercises I invented for myself as a bonus


# start

length: int = int(input("what the is meter?"))
meter = length // 100
print(f"length on meter, {meter}")
if not length % 100 == 0:
    print(f"length on cm, {length % 100}")

# stop


# start

volume:int= int(input("what the is volume?"))

liter= volume// 1000
print(f"volume on liter , {liter}")
if not volume % 1000==0:
    print(f"volume on milliliter , {volume % 1000}")

# stop


